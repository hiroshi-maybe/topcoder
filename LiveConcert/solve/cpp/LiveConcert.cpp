#include <vector>
#include <string>
using namespace std;

class LiveConcert {
public:
    int maxHappiness(vector<int> const &h,
                     vector<string> const &s) {
        return 0;
    }
};
