#include <vector>
#include <string>
using namespace std;

class CorruptedMessage {
public:
    string reconstructMessage(string const &s,
                              int k) {
        return "$";
    }
};
