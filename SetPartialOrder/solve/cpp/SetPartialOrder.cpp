#include <vector>
#include <string>
using namespace std;

class SetPartialOrder {
public:
    string compareSets(vector<int> const &a,
                       vector<int> const &b) {
        return "$";
    }
};
