#include <vector>
#include <string>
using namespace std;

class IsItASquare {
public:
    string isSquare(vector<int> const &x,
                    vector<int> const &y) {
        return "$";
    }
};
